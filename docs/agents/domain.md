# Domain Docs

This is a single-context repository.

Before working in the codebase:

- Read the root `CONTEXT.md`.
- Read relevant decisions under `docs/adr/`.
- Use the glossary’s canonical vocabulary.
- Surface conflicts with an ADR instead of silently overriding it.
- If a document is absent, continue silently; domain documentation is created only when required.
